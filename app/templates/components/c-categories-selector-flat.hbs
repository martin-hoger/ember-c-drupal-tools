{{#c-form-field-select
  model=model
  label=label
  placeholder=placeholder
  field=field
  allowClear=true
  onSelect=onSelect
  searchField=searchField
  multiple=multiple
  disabled=disabled
  options=filteredOptions as |category|
}}

  {{category.fullName}}
  
  {{#if category.distinctObjectsCount}}
    ({{category.distinctObjectsCount}})
  {{/if}}

{{/c-form-field-select}}
